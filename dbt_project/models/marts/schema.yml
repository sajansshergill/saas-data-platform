version: 2

models:
  - name: fact_revenue
    columns:
      - name: payment_id
        tests: [not_null, unique]
      - name: customer_id
        tests: [not_null]

  - name: dim_customers
    columns:
      - name: customer_id
        tests: [not_null, unique]
